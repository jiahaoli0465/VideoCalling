{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "concurrently --kill-others \"yarn watch\"  \"nodemon .\""
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.1.0",
    "tslint": "^6.1.3",
    "typescript": "^5.4.2"
  }
}
